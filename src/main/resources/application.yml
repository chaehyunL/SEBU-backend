spring:
  application:
    name: sebu-backend
  profiles:
    default: local
  jpa:
    open-in-view: false

app:
  rate-limit:
    max-requests: 30
    window: 1m
  laboratory-retention:
    days: 30
    purge-cron: "0 0 3 * * *"
    time-zone: Asia/Seoul

